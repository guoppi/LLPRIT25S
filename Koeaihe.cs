using system;

namespace Programs

    public class Solution
    {
    /* KOEAIHEET
    TYYPPIMUUNNOS
    EI POIKKEUKSIA OSIOT ELI TEHTÄVÄ 12

    MUISTISÄÄNTÖJÄ
    
            i++;                // i = i + 1; SOPII FOR LOOPPIIN ESIM (1-10), i-- TAAS ON KÄÄNTEINEN ESIM HALUTAAN ISOMMASTA PIENEMPÄÄN (10-1)
            i++; j += i;        // i = i + 1; j = j + i;
            k *= i * j; k++;    // k = k * i * j; k = k + 1; 
            j--; i += j + k;    // j = j - 1; i = i + (j + k); 
            ++j; k -= j;        // j = j + 1; k = k - j;
    
    */
    }
